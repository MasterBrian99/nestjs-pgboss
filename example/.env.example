DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=example
# Alternative to the discrete vars above: a single connection string.
# DATABASE_URL=postgres://postgres:password@localhost:5432/wavezync_test
